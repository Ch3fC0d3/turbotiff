# Assembly Review

Review can reorder or exclude pages, approve/reject joins, change depth offsets
or stretch, separate runs, choose duplicates/overlap ownership, and override
curve matches or wrap offsets. Automatic order and joins are preserved beside
review history, reviewer and time. A result becomes `export_ready` only when
every join is approved/manual and no unresolved high-severity warning remains.
