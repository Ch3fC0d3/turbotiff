# Page Ordering

Ordering uses canonical top/bottom depth, continuity error, reviewed page
numbers and deterministic tie-breaking. It evaluates complete permutations for
small page sets and reports alternative sequences. Filenames are not ordering
authority. Unknown units, non-monotonic ranges and separate run metadata must be
reviewed; uploaded-together pages are not automatically one log.
