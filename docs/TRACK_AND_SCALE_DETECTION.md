# Track and Scale Detection

Persistent vertical separators define major boundaries; close double lines are
merged before adjacent major boundaries become tracks. Minor grids are retained
inside each track. Scale classification combines spacing variation and numeric
ticks, supports reversed direction and multiple logarithmic cycles, and remains
unknown when visual evidence is insufficient. Curve mnemonic alone cannot make
a track logarithmic.
