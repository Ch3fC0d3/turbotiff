# Page Setup Review

Review retains the automatic result, reviewed values, field paths, reviewer and
timestamp. Users may correct borders, depth controls, scales, cycles, names,
units, curve selection and page classification. Only `approved` results can be
converted to tracing requests. Critical ambiguity blocks authoritative tracing;
warnings never silently change numerical settings.
